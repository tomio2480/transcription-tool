# AGENTS.md

本ファイルは，AI エージェント（Claude Code・Codex 等）が利用側リポジトリから
`transcribe-audio` を呼び出すときの約束事を定める．
本リポジトリ自体を改修するときの規律は [CLAUDE.md](CLAUDE.md) に従う．

## 📚 目次

- 🧭 本ツールの責務
- 🚦 呼び出しの手順
- 📤 出力と終了コード
- ⏱️ 処理時間と実行方法
- 🔒 録音と文字起こしの扱い
- 🩺 失敗時の対処

## 🧭 本ツールの責務

- 録音を whisper.cpp で文字起こしし，プレーンテキストの txt を生成するところまでを担う．
- 誤認識の補正・Markdown 化・要約は利用側の作業である．本ツールには頼らない．

## 🚦 呼び出しの手順

1. `transcribe-audio check` を実行し，全行が `[OK]` であることを確かめる．
   `[NG]` の行があれば，文字起こしへ進まずユーザーへ報告する．
2. `transcribe-audio transcribe --audio <録音> --output-dir <出力先>` を実行する．
   固有名詞の辞書を利用側が持つ場合は `--vocabulary <vocabulary.yml>` を付ける．
3. 標準出力の 1 行を txt のパスとして受け取り，以降の作業に使う．

- whisper-cli とモデルのパスは環境側で設定済みとして扱う．
  エージェントは `--whisper-cli` / `--model` を付けない．`.env` も作らない．
- GPU 版（Vulkan 版など）を使うかどうかは，ユーザー環境変数 `WHISPER_CLI_PATH` で決まる．
  `check` の whisper-cli 行が `source=env` なら，ユーザーが用意したバイナリが使われる．
- ユーザー環境変数を変更した直後は，エージェントを動かすアプリの再起動まで反映されないことがある．
  想定と異なる `source=` が表示されたら，その旨をユーザーへ伝える．

## 📤 出力と終了コード

- 出力先には `<録音の stem>.txt` と中間 WAV（`<stem>.16k.wav`，1 時間でおよそ 115 MB）が残る．
- 標準出力は txt のパス 1 行だけである．whisper.cpp の出力は失敗時のみ標準エラーへ出る．

表 1 終了コード．

| 終了コード | 意味 | エージェントの対応 |
|---|---|---|
| 0 | 成功 | 標準出力のパスを使う |
| 1 | 実行失敗（ffmpeg・whisper.cpp の異常終了など） | 標準エラーの末尾を添えてユーザーへ報告する |
| 2 | 入力または環境の不備 | 欠けているものを名指しで報告する．代替手段へ黙って切り替えない |

## ⏱️ 処理時間と実行方法

- 処理時間の目安は，録音長に対して GPU（Vulkan 版）で約 0.3 倍，CPU 版で約 0.9 倍である．
  モデルの読み込みに数十秒かかるため，短い録音ほど倍率は大きくなる．
- 数分を超える録音は，シェルツールの既定タイムアウトを超える．
  バックグラウンド実行にして，完了の通知を待つ．
- 実行前に `ffprobe` などで録音長を確かめ，所要時間の見込みをユーザーへ伝える．
- 同じ GPU を使う文字起こしを並列に走らせない．

## 🔒 録音と文字起こしの扱い

- 録音と txt は会話の内容を含む個人情報として扱う．
- 出力先は Git 管理外のディレクトリにする．利用側リポジトリで `.gitignore` 済みか確かめてから書き出す．
- 録音・txt・中間 WAV をコミットしない．本文を Issue や PR などの公開の場へ貼らない．
- 本文の確認をユーザーへ報告するときは，必要な範囲の要約にとどめる．

## 🩺 失敗時の対処

- Vulkan 版の whisper-cli が終了コード `-1073740791`（`0xC0000409`）で落ちるときは，GPU ドライバーが古い可能性が高い．
  ドライバーの更新を提案する．
  ユーザーの了承があれば，環境変数 `GGML_VK_DISABLE_COOPMAT=1` を付けて再実行してもよい．
- ドライバーの更新中や直後は GPU 処理が異常終了する．
  更新が済むまで再実行しない．
- 環境構築の手順は [README.md](README.md) の「🎮 AMD GPU（Vulkan）で使う」を参照する．
